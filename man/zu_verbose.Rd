% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbose.R
\name{zu_verbose}
\alias{zu_verbose}
\title{Print a trace of each request}
\usage{
zu_verbose(to = stderr(), body = TRUE)
}
\arguments{
\item{to}{A connection to write to. Defaults to \code{\link[=stderr]{stderr()}}, so a trace
never contaminates data a script is writing to stdout.}

\item{body}{Show response body sizes.}
}
\value{
A \code{\link[=zu_hooks]{zu_hooks()}} object.
}
\description{
Returns hooks that log the request lifecycle, for
\code{zu_client(hooks = zu_verbose())}.
}
\section{Credentials}{

A trace cannot leak one, and in two different ways. The request and
response lines come from §35.3 hooks, whose payloads are redacted before
any handler runs, so this function never sees a real \code{Authorization} header
— there is nothing here to get right or wrong, which is why verbose logging
is implemented as hooks rather than as its own path through the request.
The phase lines from \code{trace = TRUE} are different: they are read off the
response, so the engine redacts each URL as it records it (§42.2, D-51). A
traced URL therefore shows \verb{?access_token=<redacted>} rather than the
token, and userinfo is dropped entirely.
}

\section{Seeing the whole chain}{

Pair it with \code{trace = TRUE} on the request and the §35.3 phases appear —
DNS, connect, TLS handshake, request, headers, body — each with the time it
happened and the gap since the previous one. Without \code{trace}, the output is
the HTTP layer only.
}

\examples{
cli <- zu_client(hooks = zu_verbose(),
                 transport = zu_mock_transport(function(r) zu_response(200L)))
invisible(zu_get("https://example.test/x", client = cli))
}
\seealso{
\code{\link[=zu_resp_trace]{zu_resp_trace()}}, \code{\link[=zu_hooks]{zu_hooks()}} for your own handlers.
}
