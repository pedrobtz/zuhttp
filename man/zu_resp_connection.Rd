% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response.R
\name{zu_resp_connection}
\alias{zu_resp_connection}
\title{Connection metadata}
\usage{
zu_resp_connection(resp)
}
\arguments{
\item{resp}{A \code{zu_response}.}
}
\value{
A named list: \code{reused_connection}, \code{remote_ip}, \code{tls_protocol},
\code{tls_cipher}, \code{trust_backend}, \code{http_version}, \code{proxy_used},
\code{retries_performed} and \code{redirect_count}.
}
\description{
§35.2: what actually happened at the transport layer for this response.
Reported for the \strong{final} hop — after a redirect chain the earlier
connections are gone, and describing one of those would answer a question
nobody asked.
}
\section{Why the cipher is named and not numbered}{

\code{tls_cipher} reads \code{"ECDHE-ECDSA-CHACHA20-POLY1305"}, not \code{"0xcca9"}. The
question someone opens this list to ask is whether the connection has
forward secrecy and an AEAD mode, and the name answers it while the number
does not. Secure Transport and Schannel both report a numeric suite code;
it is mapped in C so all three backends say the same kind of thing.

\code{trust_backend} is separate from the TLS engine because §13.1 splits them —
on macOS the protocol is Secure Transport and the trust decision is
SecTrust, and "which store trusted this?" is a question users genuinely
arrive with.
}

\examples{
r <- zu_response(200L)
zu_resp_connection(r)
}
\seealso{
\code{\link[=zu_resp_timings]{zu_resp_timings()}}, \code{\link[=zu_info]{zu_info()}} for what the build can do.
}
