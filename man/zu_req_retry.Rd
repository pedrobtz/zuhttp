% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retry.R
\name{zu_req_retry}
\alias{zu_req_retry}
\title{Per-request retry settings}
\usage{
zu_req_retry(
  req,
  attempts = 3L,
  replay_safe = NULL,
  idempotency_key = NULL,
  ...
)
}
\arguments{
\item{req}{A \code{zu_request}.}

\item{attempts}{Total attempts for this request; see \code{\link[=zu_retry]{zu_retry()}}.}

\item{replay_safe}{Declare that replaying this request is safe even though
its method is not idempotent. This is a promise about the server, and
only the caller can make it.}

\item{idempotency_key}{\code{TRUE} to generate an \code{Idempotency-Key} header, or a
string to supply one. A request carrying that header is replay-safe by
§33.1, because the header is the mechanism the payment and API ecosystem
standardised on for exactly this.}

\item{...}{Further \code{\link[=zu_retry]{zu_retry()}} arguments.}
}
\value{
The request, modified.
}
\description{
The only way to make a non-idempotent request retryable. \code{zu_retry()} on a
client says \emph{how} to retry; this says \emph{whether this particular request may
be}.
}
\examples{
req <- zu_request("POST", "https://api.example.com/charges")
# A POST is never retried automatically; this is the opt-in.
req <- zu_req_retry(req, attempts = 3, idempotency_key = TRUE)
zu_req_replay_safe(req)
}
\seealso{
\code{\link[=zu_retry]{zu_retry()}}
}
