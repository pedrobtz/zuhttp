% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/json.R
\name{zu_set_json_backend}
\alias{zu_set_json_backend}
\title{Choose the JSON implementation}
\usage{
zu_set_json_backend(encode = NULL, decode = NULL)
}
\arguments{
\item{encode}{A function \verb{(x, ...)} returning a single JSON string. Must
accept \code{...}, since request-level options are forwarded to it.}

\item{decode}{A function \verb{(txt, ...)} returning an R object.}
}
\value{
The previous backend, invisibly.
}
\description{
\code{zuhttp} has no hard dependencies. \code{\link[=zu_body_json]{zu_body_json()}} and \code{\link[=zu_resp_json]{zu_resp_json()}}
use \code{jsonlite} if it is installed, and this replaces it with anything else.
}
\examples{
\dontrun{
zu_set_json_backend(
  encode = function(x, ...) RcppSimdJson::serialize(x),
  decode = function(txt, ...) RcppSimdJson::fparse(txt)
)
}
}
