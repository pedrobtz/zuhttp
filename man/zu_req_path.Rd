% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{zu_req_path}
\alias{zu_req_path}
\title{Stream the response body to a file}
\usage{
zu_req_path(req, path)
}
\arguments{
\item{req}{A \code{zu_request}.}

\item{path}{Destination path.}
}
\value{
The request, modified.
}
\description{
The composable equivalent of \code{zu_get(url, path = )}. The body is written to
a temporary file in the same directory as \code{path} and renamed once it has
arrived whole (§27.1), so a failed, cancelled or interrupted transfer never
leaves a truncated file where the caller will find it and trust it.
}
\details{
The same directory, rather than \code{tempdir()}: \code{rename()} is atomic only
within a filesystem, and a temporary directory on another mount turns the
commit into a copy that can itself fail halfway.

An existing file at \code{path} is \strong{replaced}, and only at that final step —
so a download that fails leaves whatever was already there untouched.
Setting both this and \code{\link[=zu_req_callback]{zu_req_callback()}} is an error, raised when the
request is performed: a response body has one destination.
}
\examples{
req <- zu_req_path(zu_request("GET", "https://x.test/big.bin"),
                   file.path(tempdir(), "big.bin"))
}
\seealso{
\code{\link[=zu_body_file]{zu_body_file()}}, which is the opposite direction — a request body
read \emph{from} a file.
}
