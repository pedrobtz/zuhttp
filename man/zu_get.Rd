% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{zu_get}
\alias{zu_get}
\title{Perform an HTTP GET}
\usage{
zu_get(
  url,
  timeout = 30,
  follow_redirects = 1L,
  verify = TRUE,
  max_body = 16 * 1024^2,
  user_agent = NULL
)
}
\arguments{
\item{url}{A single URL. Only \verb{http://} and \verb{https://} are accepted.}

\item{timeout}{Total seconds for the whole operation, redirects included.}

\item{follow_redirects}{Maximum redirects to follow. \code{0} returns the 3xx.}

\item{verify}{Verify the peer certificate and hostname. Leave this \code{TRUE};
it exists so tests can reach a server with a self-signed certificate.}

\item{max_body}{Maximum decoded body size in bytes.}

\item{user_agent}{\code{User-Agent} to send.}
}
\value{
A \code{zu_response} object. Use \code{\link[=zu_resp_status]{zu_resp_status()}}, \code{\link[=zu_resp_body]{zu_resp_body()}},
\code{\link[=zu_resp_headers]{zu_resp_headers()}} and \code{\link[=zu_resp_url]{zu_resp_url()}} to read it.
}
\description{
The first end-to-end path through zuhttp: URL parsing, TCP, TLS with
certificate and hostname verification, request construction, strict response
framing, chunked decoding, transparent gzip, one redirect, and a total
deadline.
}
\section{Platform status}{

The long-term design uses each platform's native TLS stack. Until those
backends land this package links OpenSSL everywhere, and \strong{on Windows that
means HTTPS cannot verify certificates}: OpenSSL there looks for a CA
bundle at a compiled-in path that does not exist and does not consult the
Windows certificate store. \code{zu_get()} on Windows therefore fails with
\code{zu_tls_certificate_error} until the Schannel backend arrives.
}

\examples{
# Requires network access, so it is not run during checks.
\dontrun{
r <- zu_get("https://example.com")
zu_resp_status(r)
zu_resp_text(r)
}
}
