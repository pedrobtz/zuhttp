% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool.R
\name{zu_pool}
\alias{zu_pool}
\title{Connection pool settings}
\usage{
zu_pool(max_idle = 16L, max_per_host = 4L, idle_timeout = 30)
}
\arguments{
\item{max_idle}{Maximum idle connections kept across all hosts.}

\item{max_per_host}{Maximum idle connections for one scheme/host/port.}

\item{idle_timeout}{Seconds an unused connection may sit in the pool.}
}
\value{
A \code{zu_pool_config}, for \code{zu_client(pool = )}. \code{zu_client(pool = NULL)} disables reuse entirely, which is what one-shot calls want.
}
\description{
Passed to \code{\link[=zu_client]{zu_client()}} as \verb{pool =}. The defaults are §26.2's deliberately
conservative ones: a pool that is slightly too eager to discard costs
latency, while one that is slightly too eager to reuse corrupts responses.
}
\section{What is never shared}{

Two requests reuse a connection only if scheme, host, port, proxy identity
\emph{including credentials}, and the whole TLS configuration match (§26.1).
That is enforced in C, on every acquisition, by comparing the key field by
field — so deriving a client with \code{\link[=zu_client_update]{zu_client_update()}} can never cause a
request with \code{verify = FALSE} to travel over a connection established with
verification on, even though the derived client shares its parent's pool.
}

\examples{
api <- zu_client(pool = zu_pool(max_idle = 4, idle_timeout = 10))
zu_pool_stats(api)
}
\seealso{
\code{\link[=zu_pool_stats]{zu_pool_stats()}} to see whether reuse is actually happening.
}
