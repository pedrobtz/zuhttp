% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{zu_req_callback}
\alias{zu_req_callback}
\title{Stream the response body to a callback}
\usage{
zu_req_callback(req, f)
}
\arguments{
\item{req}{A \code{zu_request}.}

\item{f}{A function of one argument (a raw vector). Return \code{FALSE} to stop.}
}
\value{
The request, modified.
}
\description{
\code{f} is called with each decoded chunk as a raw vector, as it arrives.
Returning \code{FALSE} stops the transfer cleanly (§27.2); the connection is
then not reused, because its framing position is no longer known (§26.3).
}
\section{If your callback raises an error}{

You get \emph{your} error, not a transport error wrapping it (§27.3). The
callback runs inside \code{R_tryCatch()}, the native read loop unwinds normally
so the socket and decompressor are released, and only then is the original
condition re-signalled. That ordering is the whole point: a longjmp
straight out of the read loop would leak the connection.
}

\section{What you may not do inside it}{

Issue another request on the \strong{same} client (§27.4). That would deadlock
on a pool slot or interleave writes onto the connection currently being
read, so it raises an error naming the problem instead. A request through a
\emph{different} client is unrestricted.
}

\examples{
total <- 0
req <- zu_req_callback(zu_request("GET", "https://x.test/big"),
                       function(chunk) total <<- total + length(chunk))
}
\seealso{
\code{\link[=zu_req_path]{zu_req_path()}} to stream to a file instead.
}
