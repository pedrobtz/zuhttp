% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response.R
\name{zu_resp_text}
\alias{zu_resp_text}
\title{The response body as text}
\usage{
zu_resp_text(resp, encoding = NULL, on_invalid = c("error", "substitute"))
}
\arguments{
\item{resp}{A \code{zu_response}.}

\item{encoding}{Encoding to assume, overriding the chain above.}

\item{on_invalid}{\code{"error"} raises \code{zu_body_decode_error} on bytes that are
not valid in the chosen encoding; \code{"substitute"} opts into lossy
conversion. Silent corruption is not on the menu.}
}
\value{
A single UTF-8 string.
}
\description{
Character encoding is decided by this chain (§31.7):
}
\details{
\enumerate{
\item \code{encoding} if you pass one;
\item the \code{charset} parameter of \code{Content-Type}, if \code{iconv()} knows it;
\item a UTF-8, UTF-16LE or UTF-16BE byte-order mark, which is then stripped;
\item otherwise \strong{UTF-8} — not the ISO-8859-1 that RFC 7231 nominally implies.
That default is a historical artefact; UTF-8 is right far more often, and
being wrong the other way produces mojibake users blame on the server.
}

For \code{application/json}, RFC 8259 mandates UTF-8, so steps 2–4 are skipped.

The result is always marked UTF-8.
}
\examples{
zu_resp_text(zu_response(200L, c("Content-Type" = "text/plain"), "hi"))
}
