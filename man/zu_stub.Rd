% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{zu_stub}
\alias{zu_stub}
\title{Match a request and return a canned response}
\usage{
zu_stub(
  response,
  method = NULL,
  url = NULL,
  regex = NULL,
  headers = NULL,
  body = NULL,
  times = Inf
)
}
\arguments{
\item{response}{A \code{\link[=zu_response]{zu_response()}}, or a function of one request returning
one. The function form is for stubs whose reply depends on the request.}

\item{method}{HTTP method to match, case-insensitive. \code{NULL} matches any.}

\item{url}{URL to match. A plain string must match exactly \emph{after} client
merging — that is, the absolute URL with the query attached. Use \code{regex}
for anything looser.}

\item{regex}{A regular expression matched against the URL, as an
alternative to \code{url}.}

\item{headers}{Named character vector of headers that must be present with
these values. Names are matched case-insensitively (§18.3); headers not
named here are ignored.}

\item{body}{Raw or character body that must match exactly.}

\item{times}{Maximum number of times this stub may match; \code{Inf} by default.
A stub with \code{times = 1} is how you assert a request is not retried.}
}
\value{
A \code{zu_stub}, for \code{\link[=zu_mock_transport]{zu_mock_transport()}}.
}
\description{
A stub pairs a matcher with a response. Every supplied criterion must match
(they are ANDed); an omitted criterion matches anything.
}
\examples{
stub <- zu_stub(zu_response(201L), method = "POST", regex = "/users$")
api  <- zu_client(transport = zu_mock_transport(stub))
zu_resp_status(zu_perform(zu_request("POST", "https://x.test/users"),
                          client = api))
}
\seealso{
\code{\link[=zu_mock_transport]{zu_mock_transport()}}
}
