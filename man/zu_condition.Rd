% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{zu_condition}
\alias{zu_condition}
\title{Construct a zuhttp condition}
\usage{
zu_condition(
  code,
  message,
  url = NULL,
  phase = NULL,
  backend = NULL,
  backend_code = NULL,
  request = NULL,
  response = NULL,
  call = sys.call(-1)
)
}
\arguments{
\item{code}{Integer code or class name, e.g. \code{"zu_tls_certificate_error"}.}

\item{message}{The human-readable message (§34.4: what was attempted, what
failed, and the most likely fix).}

\item{url}{Optional URL. It is \strong{redacted} before being stored (§42.2), so
a condition object can be printed or logged safely.}

\item{phase}{One of \code{"dns"}, \code{"connect"}, \code{"tls"}, \code{"write"}, \code{"ttfb"},
\code{"read"}, \code{"decode"}, or \code{NULL}.}

\item{backend, backend_code}{The platform backend and its native code, where
useful. Present but never leading (§34.4).}

\item{request, response}{Objects where they exist.}

\item{call}{The originating call.}
}
\value{
A condition object inheriting from the §34.1 classes, plus \code{error}
and \code{condition}.
}
\description{
Mostly internal, but exported so that package authors building on zuhttp can
raise conditions their users can catch with the same handlers.
}
