% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response.R
\name{zu_resp_trace}
\alias{zu_resp_trace}
\title{The event trace for a request}
\usage{
zu_resp_trace(resp)
}
\arguments{
\item{resp}{A \code{zu_response}.}
}
\value{
A data frame of \code{event}, \code{at_ms}, \code{n} and \code{detail}, or \code{NULL} if
the request was not traced. \code{at_ms} is milliseconds from the start of the
operation, so the gaps between rows are the phases.
}
\description{
§35.3's lifecycle events, in order, when the request was made with
\code{trace = TRUE}. This is the DNS-to-body view: the phases a timing summary
can only total up.
}
\section{Why a trace is collected rather than streamed}{

These events happen inside the connect and handshake paths, where an R
error raised from a callback would longjmp past a half-built socket and a
live TLS context — the §27.3 hazard, at six more call sites. A trace does
not need to be live to be useful, so the engine appends to a fixed log and
R renders it afterwards. Tracing off costs one NULL check per event.
}

\section{Credentials}{

The \code{detail} of a URL-bearing event is redacted by the engine as it is
recorded (§42.2): userinfo is dropped and a secret query parameter reads
\verb{<redacted>}. It has to happen there rather than here — the trace is stored
on the response, so anything this function could filter would already have
been written down.
}

\examples{
r <- zu_get("https://example.com", trace = TRUE)
zu_resp_trace(r)
}
\seealso{
\code{\link[=zu_resp_timings]{zu_resp_timings()}} for the summary, \code{\link[=zu_verbose]{zu_verbose()}} for a printed
narration.
}
