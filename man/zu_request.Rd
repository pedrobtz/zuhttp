% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{zu_request}
\alias{zu_request}
\title{Build a request without performing it}
\usage{
zu_request(method, url)
}
\arguments{
\item{method}{HTTP method. Any token is allowed — \code{"PROPFIND"} works —
and it is upper-cased.}

\item{url}{An absolute URL, or a path to be joined to a client's \code{base_url}
at perform time.}
}
\value{
A \code{zu_request}.
}
\description{
Method and URL are request identity, so both are given here rather than
configured later (§31.5). The result is an inert value; \code{\link[=zu_perform]{zu_perform()}} is
the only thing that touches the network.
}
\examples{
req <- zu_request("POST", "https://api.example.com/users")
req <- zu_body_json(req, list(name = "Alice"))
req
}
\seealso{
\code{\link[=zu_headers]{zu_headers()}}, \code{\link[=zu_query]{zu_query()}}, \code{\link[=zu_body_json]{zu_body_json()}}, \code{\link[=zu_perform]{zu_perform()}}
}
