% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topics.R
\name{zuhttp_fork}
\alias{zuhttp_fork}
\title{HTTPS in a forked process}
\description{
Why \code{zuhttp} raises \code{zu_fork_error} inside \code{mclapply()} on macOS, and what
to use instead.
}
\details{
\code{parallel::mclapply()}, \code{parallel::mcparallel()} and \code{future}'s \code{multicore}
plan all work by forking the R process. Two separate things go wrong when a
forked child makes an HTTPS request.

\strong{The trust evaluator does not survive \code{fork()} (macOS).}
Security.framework opens an XPC connection to \code{trustd} the first time it
evaluates trust, and that connection is not inherited. A child that tries to
use it is killed outright — measured during the design spike: a parent that
had made one HTTPS request, then forked, produced children killed by
\code{SIGSEGV}; a parent that had made none produced children that survived. It
is fork-\emph{after-use}, not fork-at-all, which is why the failure looks
intermittent and why it usually appears only once a script grows a warm-up
request.

\code{zuhttp} therefore records the process ID that armed the trust evaluator and
refuses to evaluate trust in a different one, raising \code{zu_fork_error}. That
does not make forked HTTPS work. It makes it diagnosable: an error naming
the cause instead of a worker vanishing.

\strong{Inherited connections.} A forked child also inherits the parent's open
sockets, pooled TLS sessions included. If both processes write into one TLS
session the session is corrupted, and the resulting failure is
nondeterministic and appears far from its cause. The same PID guard drops
every inherited connection on first use in the child, without a graceful
TLS shutdown — a polite close would write to a socket the parent still owns.
\code{zu_pool_stats()} reports these as \code{discarded_fork} and \code{forks_detected}.
}
\section{What to use instead}{

\code{PSOCK} clusters and \code{future::plan("multisession")} start fresh R processes
with \code{exec()} rather than forking, so neither hazard exists:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cl <- parallel::makeCluster(4)
parallel::clusterEvalQ(cl, library(zuhttp))
parallel::parLapply(cl, urls, function(u) zu_get(u, path = basename(u)))
parallel::stopCluster(cl)
}\if{html}{\out{</div>}}

Plain HTTP in a forked child does not touch the trust evaluator, so it is
not subject to the first hazard — but it is still subject to the second, and
relying on that distinction is not worth the surprise when a URL later
becomes \verb{https://}.

\code{zuhttp} performs one request at a time; there is no built-in concurrency,
so parallelism is always the caller's to arrange.
}

\seealso{
\code{\link[=zu_pool_stats]{zu_pool_stats()}} for the counters, \code{\link[=zu_client]{zu_client()}} for the pool.
}
