% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{zu_client}
\alias{zu_client}
\title{Create a reusable client}
\usage{
zu_client(
  base_url = NULL,
  headers = NULL,
  query = NULL,
  timeout = NULL,
  redirects = NULL,
  verify = NULL,
  max_body = NULL,
  user_agent = NULL,
  check = NULL,
  decode = NULL,
  transport = zu_native_transport(),
  pool = zu_pool(),
  retry = NULL,
  middleware = NULL,
  hooks = NULL,
  proxy = NULL,
  tls = NULL
)
}
\arguments{
\item{base_url}{Optional base URL, so that \code{zu_get("/users", client = api)}
works. Joining is textual, not RFC 3986 resolution: see Details.}

\item{headers}{Named character vector of default headers.}

\item{query}{Named list of default query parameters.}

\item{timeout}{Total seconds for a request, redirects included.}

\item{redirects}{Maximum redirects to follow. \code{0} returns the 3xx itself.}

\item{verify}{Verify the certificate and hostname. Leave this \code{TRUE}.}

\item{max_body}{Maximum decoded body size in bytes.}

\item{user_agent}{Default \code{User-Agent}.}

\item{check}{Raise a condition for 4xx and 5xx responses (§31.14). \code{FALSE}
returns the response whatever its status.}

\item{decode}{Decompress the body transparently (§21.2).}

\item{transport}{The object that actually performs requests. Defaults to
\code{\link[=zu_native_transport]{zu_native_transport()}}; \code{\link[=zu_mock_transport]{zu_mock_transport()}} replaces the network in
tests.}

\item{pool}{Connection reuse settings from \code{\link[=zu_pool]{zu_pool()}}, or \code{NULL} to open a
fresh connection for every request.}

\item{retry}{A \code{\link[=zu_retry]{zu_retry()}} policy. The default retries nothing.}

\item{middleware}{A function of \verb{(req, next_fn)}, or a list of them,
wrapping request execution (§31.13). The first is outermost.}

\item{hooks}{Lifecycle observers from \code{\link[=zu_hooks]{zu_hooks()}}.}

\item{proxy}{Proxy URL, e.g. \code{"http://proxy:3128"}. Leave unset to honour
the environment (§20.1); pass \code{FALSE} to force a direct connection.
\strong{Not \code{NULL}} — under the §31.9 merge rules \code{NULL} means "reset to the
package default", which for a proxy is "consult the environment". \code{FALSE}
is the only spelling that can mean "definitely do not proxy" without
making this one argument an exception to a rule every other policy
argument follows.}

\item{tls}{Certificate trust settings from \code{\link[=zu_tls]{zu_tls()}}.}
}
\value{
A \code{zu_client} object.
}
\description{
A client carries what many requests share: a base URL, default headers and
query parameters, policy defaults, and the connection pool.
It is never the first argument of a request function — see below.
}
\details{
\code{base_url} is \strong{joined} to a request path, not resolved against it: a
client with \code{base_url = "https://api.example.com/v1"} and a request path of
\code{"/users"} produces \verb{https://api.example.com/v1/users}. RFC 3986 resolution
would give \verb{https://api.example.com/users} — the \verb{/v1} silently dropped —
which is the single most common surprise in libraries that resolve here.
An absolute request URL ignores \code{base_url} entirely.
}
\section{The client is never the first argument}{

\code{zu_get(url, ..., client = api)}, never \code{zu_get(api, url)}. Overloading
argument one by type breaks autocomplete, makes dispatch murky, and turns a
misplaced argument into a confusing error instead of a clear one (§31.3).
For client-first phrasing there are thin wrappers: \code{api |> zu_client_get("/users")}.
}

\examples{
api <- zu_client(
  base_url = "https://api.example.com",
  headers  = c(Accept = "application/json")
)
api
}
\seealso{
\code{\link[=zu_client_update]{zu_client_update()}} to derive a client without mutating it.
}
