% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{zu_methods}
\alias{zu_methods}
\alias{zu_get}
\alias{zu_head}
\alias{zu_post}
\alias{zu_put}
\alias{zu_patch}
\alias{zu_delete}
\title{Perform a request in one call}
\usage{
zu_get(
  url,
  query = NULL,
  headers = NULL,
  timeout = NULL,
  redirects = NULL,
  verify = NULL,
  max_body = NULL,
  user_agent = NULL,
  check = NULL,
  decode = NULL,
  retry = NULL,
  path = NULL,
  callback = NULL,
  proxy = NULL,
  tls = NULL,
  trace = FALSE,
  client = zu_default_client()
)

zu_head(
  url,
  query = NULL,
  headers = NULL,
  timeout = NULL,
  redirects = NULL,
  verify = NULL,
  max_body = NULL,
  user_agent = NULL,
  check = NULL,
  decode = NULL,
  retry = NULL,
  path = NULL,
  callback = NULL,
  proxy = NULL,
  tls = NULL,
  trace = FALSE,
  client = zu_default_client()
)

zu_post(
  url,
  query = NULL,
  headers = NULL,
  body = NULL,
  json = NULL,
  form = NULL,
  file = NULL,
  timeout = NULL,
  redirects = NULL,
  verify = NULL,
  max_body = NULL,
  user_agent = NULL,
  check = NULL,
  decode = NULL,
  retry = NULL,
  path = NULL,
  callback = NULL,
  proxy = NULL,
  tls = NULL,
  trace = FALSE,
  client = zu_default_client()
)

zu_put(
  url,
  query = NULL,
  headers = NULL,
  body = NULL,
  json = NULL,
  form = NULL,
  file = NULL,
  timeout = NULL,
  redirects = NULL,
  verify = NULL,
  max_body = NULL,
  user_agent = NULL,
  check = NULL,
  decode = NULL,
  retry = NULL,
  path = NULL,
  callback = NULL,
  proxy = NULL,
  tls = NULL,
  trace = FALSE,
  client = zu_default_client()
)

zu_patch(
  url,
  query = NULL,
  headers = NULL,
  body = NULL,
  json = NULL,
  form = NULL,
  file = NULL,
  timeout = NULL,
  redirects = NULL,
  verify = NULL,
  max_body = NULL,
  user_agent = NULL,
  check = NULL,
  decode = NULL,
  retry = NULL,
  path = NULL,
  callback = NULL,
  proxy = NULL,
  tls = NULL,
  trace = FALSE,
  client = zu_default_client()
)

zu_delete(
  url,
  query = NULL,
  headers = NULL,
  body = NULL,
  json = NULL,
  form = NULL,
  file = NULL,
  timeout = NULL,
  redirects = NULL,
  verify = NULL,
  max_body = NULL,
  user_agent = NULL,
  check = NULL,
  decode = NULL,
  retry = NULL,
  path = NULL,
  callback = NULL,
  proxy = NULL,
  tls = NULL,
  trace = FALSE,
  client = zu_default_client()
)
}
\arguments{
\item{url}{An absolute URL, or a path joined to the client's \code{base_url}.}

\item{query}{Named list of query parameters. Merged with the client's.}

\item{headers}{Named character vector. Merged with the client's; \code{NA}
removes an inherited header.}

\item{timeout}{Total seconds, redirects included.}

\item{redirects}{Maximum redirects to follow.}

\item{verify}{Verify the certificate and hostname. Leave this \code{TRUE}.}

\item{max_body}{Maximum decoded body size in bytes.}

\item{user_agent}{\code{User-Agent} to send.}

\item{check}{Raise a condition for 4xx and 5xx (§31.14). \code{FALSE} returns the
response whatever its status.}

\item{decode}{Decompress transparently. \code{FALSE} returns the wire bytes and
asks the server not to encode (§21.2).}

\item{retry}{A \code{\link[=zu_retry]{zu_retry()}} policy for this request. Overrides the
client's; \code{NULL} inherits it (§31.9).}

\item{path}{Write the response body to this file instead of holding it in
memory (§27). The download is written beside the destination and renamed
on success, so a failed or interrupted transfer never leaves a truncated
file at \code{path}. An existing file at \code{path} is \strong{replaced}, and only once
the body has arrived whole. Note what that does and does not cover: a
\emph{transfer} that fails leaves the old file untouched, but a request that
completes with a non-2xx status is a successful transfer of an error
page, so a 404 body is written and does replace it — \code{check} raises
afterwards, too late to prevent that. Mutually exclusive with
\code{callback}: a response body has one destination. Note the asymmetry with \code{file}, which
is a request \emph{body} source (§31.6): \code{path} is where the response goes,
\code{file} is where a
request body comes from.}

\item{callback}{A function of one argument called with each decoded chunk
as it arrives (§27). Returning \code{FALSE} stops the transfer. See
\code{\link[=zu_req_callback]{zu_req_callback()}} for what happens when it raises an error.}

\item{proxy}{Proxy URL for this request, or \code{FALSE} for a direct
connection. See \code{\link[=zu_client]{zu_client()}} for why disabling is \code{FALSE}, not \code{NULL}.}

\item{tls}{Certificate trust settings from \code{\link[=zu_tls]{zu_tls()}} for this request.}

\item{trace}{Collect a §35.3 event trace, readable with
\code{\link[=zu_resp_trace]{zu_resp_trace()}}. Off by default.}

\item{client}{A \code{zu_client}. Always named.}

\item{body}{Raw vector or string, sent as-is.}

\item{json}{An object to serialise as JSON. Sets \code{Content-Type: application/json} unless you set that header yourself.}

\item{form}{A named list, sent as \code{application/x-www-form-urlencoded}.}

\item{file}{A path whose contents become the body.}
}
\value{
A \code{zu_response}.
}
\description{
The short form. \code{zu_get(url)} is the whole API for the common case; the
arguments below are the same options \code{\link[=zu_client]{zu_client()}} takes, applied to this
request only.
}
\details{
Passing \code{NULL} for a policy argument resets it to the \emph{package} default
rather than to the client's value — that is the documented way to escape a
client default (§31.9). Omitting it inherits the client's.
}
\examples{
\dontrun{
zu_get("https://api.example.com/search", query = list(q = "HTTP", limit = 20))
zu_post("https://api.example.com/users", json = list(name = "Alice"))

# Download straight to disk: the body never passes through memory, and the
# file appears at its destination only once it has arrived whole.
r <- zu_get("https://example.com/big.bin", path = "big.bin")
zu_resp_path(r)                       # "big.bin"
zu_resp_header(r, "content-type")     # the response is still a response

# A non-2xx body is still a body: this raises AND leaves the 404 page at
# out.bin, replacing whatever was there. §27.1's atomicity covers a failed
# transfer, and a 404 is a successful transfer of an error page. Check
# first if the destination matters.
r <- zu_get("https://example.com/missing", path = "out.bin", check = FALSE)
if (zu_resp_ok(r)) file.rename("out.bin", "wanted.bin")
}
}
\seealso{
\code{\link[=zu_request]{zu_request()}} and \code{\link[=zu_perform]{zu_perform()}} to build a request without
performing it.
}
