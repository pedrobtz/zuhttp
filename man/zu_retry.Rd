% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retry.R
\name{zu_retry}
\alias{zu_retry}
\title{Retry policy}
\usage{
zu_retry(
  attempts = 3L,
  backoff = c("exponential", "constant"),
  base = 1,
  max_delay = 60,
  jitter = TRUE,
  retry_after = TRUE,
  max_retry_after = 60,
  attempt_timeout = NULL,
  on = NULL
)
}
\arguments{
\item{attempts}{Total attempts \emph{including the first}, so \code{attempts = 1}
means no retrying and \code{attempts = 3} means at most two retries. (An
earlier draft of the design used \code{attempts} and \code{max_attempts} with
meanings differing by one; there is one name and one meaning.)}

\item{backoff}{\code{"exponential"} or \code{"constant"}.}

\item{base}{Seconds for the first backoff interval.}

\item{max_delay}{Ceiling for a single computed backoff, in seconds.}

\item{jitter}{Randomise each delay over \verb{[0, delay]} ("full jitter").
On by default: synchronised retries from many R sessions are a real
thundering-herd source (§33.3).}

\item{retry_after}{Honour a \code{Retry-After} response header.}

\item{max_retry_after}{Clamp for \code{Retry-After}, in seconds. A hostile or
misconfigured server must not be able to pin an R session for hours.}

\item{attempt_timeout}{Optional per-attempt bound in seconds. The \code{total}
timeout still bounds the whole call (§24.3); this bounds one try.}

\item{on}{Extra HTTP statuses to treat as retryable, beyond §33.2's
408, 429, 500, 502, 503 and 504.}
}
\value{
A \code{zu_retry_policy}, for \code{zu_client(retry = )} or
\code{\link[=zu_req_retry]{zu_req_retry()}}.
}
\description{
Retrying is \strong{off by default}: the package default is \code{attempts = 1}.
}
\examples{
zu_retry(attempts = 3)
zu_client(retry = zu_retry(attempts = 5, max_delay = 10))
}
\seealso{
\code{\link[=zu_req_retry]{zu_req_retry()}} for per-request opt-in, including the
replay-safety override that a POST needs.
}
