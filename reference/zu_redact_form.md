# Redact a form-encoded request body

Redact a form-encoded request body

## Usage

``` r
zu_redact_form(body)
```

## Arguments

- body:

  A single `application/x-www-form-urlencoded` string.

## Value

The redacted string.
